<%= form_with(model: @help, local: true) do |form| %>
  <% if @help.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(help.errors.count, "error") %> prohibited this help from being saved:</h2>

      <ul>
      <% help.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :genre %>
    <%= form.text_field :genre, id: :help_genre %>
  </div>

  <div class="field">
    <%= form.label :task %>
    <%= form.text_field :task, id: :help_task %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country, id: :help_country %>
  </div>

  <div class="field">
    <%= form.label :timeframe %>
    <%= form.text_field :timeframe, id: :help_timeframe %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_area :comments, id: :help_comments %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
